Placeholder. The homepage HTML references this companion assets folder (JS/CSS/images
from the browser's "Save Page As → Webpage, Complete" capture). Upload the real folder's
contents here to make the page render fully offline. See ../README.md.
